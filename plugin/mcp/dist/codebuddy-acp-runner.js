import{spawn}from"node:child_process";import{appendFileSync,existsSync,readFileSync,renameSync,watch,writeFileSync}from"node:fs";import path from"node:path";function normalizeStringArray(value){if(Array.isArray(value))return value.filter(entry=>typeof entry==="string");return typeof value==="string"&&value.trim()!==""?[value]:[]}function readOptions(){const index=process.argv.indexOf("--options-file");if(index<0||!process.argv[index+1])throw new Error("Missing --options-file for CodeBuddy ACP runner.");const options=JSON.parse(readFileSync(process.argv[index+1],"utf8").replace(/^\uFEFF/,""));options.allowed_paths=normalizeStringArray(options.allowed_paths);options.forbidden_paths=normalizeStringArray(options.forbidden_paths);options.writable_paths=normalizeStringArray(options.writable_paths);options.required_checks=normalizeStringArray(options.required_checks);if(options.schema!=="codex-praetor-codebuddy-acp-runner/v1")throw new Error("Unsupported CodeBuddy ACP runner options schema.");if(![options.job_id,options.cwd,options.prompt,options.launcher_path,options.cli_path,options.model,options.state_path,options.trace_path].every(Boolean)||!Number.isFinite(options.max_stall_seconds)||options.max_stall_seconds<30){throw new Error("CodeBuddy ACP runner options are incomplete.")}return options}function writeState(options,state){const payload={schema:"codex-praetor-codebuddy-acp-session/v1",job_id:options.job_id,connection_mode:"codebuddy_acp",...state,updated_at:new Date().toISOString()};const temporary=`${options.state_path}.${process.pid}.tmp`;writeFileSync(temporary,`${JSON.stringify(payload)}
`,"utf8");renameSync(temporary,options.state_path)}function trace(options,event,data={}){appendFileSync(options.trace_path,`${JSON.stringify({at:new Date().toISOString(),event,...data})}
`,"utf8")}function normalizeRelative(value){return value.replaceAll("\\","/").replace(/^\.\//,"")}function globMatches(pattern,value){const escaped=normalizeRelative(pattern).replace(/[|\\{}()[\]^$+?.]/g,"\\$&").replaceAll("**","::DOUBLE_STAR::").replaceAll("*","[^/]*").replaceAll("::DOUBLE_STAR::",".*");return new RegExp(`^${escaped}$`,"i").test(normalizeRelative(value))}function relativeInside(root,candidate){if(typeof candidate!=="string"||candidate.trim()==="")return"";const resolvedRoot=path.resolve(root);const resolved=path.resolve(root,candidate);if(resolved!==resolvedRoot&&!resolved.startsWith(`${resolvedRoot}${path.sep}`))return void 0;const relative=normalizeRelative(path.relative(resolvedRoot,resolved));return relative||"."}function pathAllowed(options,candidate,write=false){const relative=relativeInside(options.cwd,candidate);if(relative===void 0)return false;if(relative==="")return true;if(options.forbidden_paths.some(entry=>globMatches(entry,relative)))return false;const allowlist=write?options.writable_paths:options.allowed_paths;return allowlist.some(entry=>globMatches(entry,relative))}function pathScope(options,candidate,write=false){const relative=relativeInside(options.cwd,candidate);if(relative===void 0)return"outside_worktree";if(relative==="")return"no_path";if(options.forbidden_paths.some(entry=>globMatches(entry,relative)))return"forbidden_path";const allowlist=write?options.writable_paths:options.allowed_paths;return allowlist.some(entry=>globMatches(entry,relative))?"allowed_path":"not_allowlisted"}function requestedPath(params={}){const raw=params.path??params.filePath??params.toolCall?.rawInput?.file_path;return typeof raw==="string"?raw:""}function requestedCommand(params={}){const raw=params.toolCall;const input=raw?.rawInput??params;const command=typeof input.command==="string"?input.command.trim():"";const args=Array.isArray(input.args)?input.args.filter(entry=>typeof entry==="string"):[];return[command,...args].filter(Boolean).join(" ").trim()}var AcpClient=class{constructor(options){this.options=options;this.child=spawn(options.launcher_path,[options.cli_path,"--acp","-y","--setting-sources","project","--model",options.model],{cwd:options.cwd,stdio:["pipe","pipe","pipe"],windowsHide:true});const stdout=this.child.stdout;const stderr=this.child.stderr;if(!stdout||!stderr||!this.child.stdin)throw new Error("ACP runner could not create stdio pipes.");stdout.setEncoding("utf8");stderr.setEncoding("utf8");stdout.on("data",chunk=>this.consume(String(chunk)));stderr.on("data",chunk=>trace(this.options,"provider_stderr",{bytes:Buffer.byteLength(String(chunk),"utf8")}));this.child.on("error",error=>trace(this.options,"spawn_error",{message:error.message}))}options;pending=new Map;terminalProcesses=new Map;child;nextId=1;buffer="";structuredEvents=0;boundaryDenials=0;cancelRequested=false;cancelAcknowledged=false;sessionId="";terminalStopReason="";terminalText="";agentMessages=new Map;lastAgentMessageId="";lastEventType="";lastProgressAt=new Date().toISOString();stopReason;stallTimer;currentState(state,error){writeState(this.options,{state,session_id:this.sessionId||void 0,structured_events:this.structuredEvents,boundary_denials:this.boundaryDenials,cancel_requested:this.cancelRequested,cancel_acknowledged:this.cancelAcknowledged,terminal_stop_reason:this.terminalStopReason||void 0,last_event_type:this.lastEventType||void 0,last_progress_at:this.lastProgressAt,stop_reason:this.stopReason,error})}send(message){trace(this.options,"client_message",{method:message.method??"response",id:message.id??null});if(!this.child.stdin)throw new Error("ACP server stdin is unavailable.");this.child.stdin.write(`${JSON.stringify({jsonrpc:"2.0",...message})}
`)}request(method,params){const id=this.nextId++;this.send({id,method,params});return new Promise((resolve,reject)=>this.pending.set(id,{resolve,reject}))}deny(id,message){this.boundaryDenials+=1;this.send({id,error:{code:-32001,message}})}allowPermission(id,allowed){if(!allowed)this.boundaryDenials+=1;this.send({id,result:{outcome:{outcome:"selected",optionId:allowed?"allow":"reject"}}})}async handleRequest(message){const params=message.params??{};const requestId=message.id;if(requestId===void 0||!message.method)return;const filePath=requestedPath(params);if(message.method==="session/request_permission"){const command=requestedCommand(params);const write=String(params.toolCall?.rawInput?.tool??"").toLowerCase().includes("write");const allowed=filePath?pathAllowed(this.options,filePath,write):command!==""&&this.options.required_checks.includes(command);this.allowPermission(requestId,allowed);trace(this.options,"permission",{allowed,path_scope:filePath?pathScope(this.options,filePath,write):"no_path",has_command:Boolean(command)});this.currentState("running");return}if(message.method==="fs/read_text_file"){if(!pathAllowed(this.options,filePath,false)){this.deny(requestId,"Read outside the declared Codex worktree contract is denied.");this.currentState("running");return}try{this.send({id:requestId,result:{content:readFileSync(path.resolve(this.options.cwd,filePath),"utf8")}})}catch(error){this.deny(requestId,error instanceof Error?error.message:String(error))}return}if(message.method==="fs/write_text_file"){if(!pathAllowed(this.options,filePath,true)){this.deny(requestId,"Write outside the declared Codex worktree contract is denied.");this.currentState("running");return}const content=params.content??params.text;if(typeof content!=="string"){this.deny(requestId,"ACP write request omitted textual content.");return}try{writeFileSync(path.resolve(this.options.cwd,filePath),content,"utf8");this.send({id:requestId,result:null})}catch(error){this.deny(requestId,error instanceof Error?error.message:String(error))}return}if(message.method==="terminal/create"){const command=requestedCommand(params);if(!this.options.required_checks.includes(command)){this.deny(requestId,"Terminal command is not an exact declared deterministic check.");this.currentState("running");return}const terminalId=`codex-praetor-${this.terminalProcesses.size+1}`;const[executable,...args]=command.split(/\s+/);const process2=spawn(executable,args,{cwd:this.options.cwd,windowsHide:true,stdio:["ignore","pipe","pipe"]});this.terminalProcesses.set(terminalId,process2);process2.stdout?.on("data",data=>this.send({method:"terminal/output",params:{terminalId,data:String(data)}}));process2.stderr?.on("data",data=>this.send({method:"terminal/output",params:{terminalId,data:String(data)}}));process2.on("exit",exitCode=>this.send({method:"terminal/exit",params:{terminalId,exitCode:exitCode??-1}}));this.send({id:requestId,result:{terminalId}});trace(this.options,"terminal_started",{command});return}if(message.method==="terminal/kill"){const terminalId=typeof params.terminalId==="string"?params.terminalId:"";const process2=this.terminalProcesses.get(terminalId);if(process2)process2.kill();this.send({id:requestId,result:null});return}this.deny(requestId,`Unsupported ACP client request: ${message.method}`);this.currentState("running")}handle(message){if(typeof message.id==="number"&&(message.result!==void 0||message.error!==void 0)&&!message.method){const pending=this.pending.get(message.id);if(pending){this.pending.delete(message.id);message.error===void 0?pending.resolve(message.result):pending.reject(new Error(JSON.stringify(message.error)))}return}if(typeof message.id==="number"&&message.method){void this.handleRequest(message);return}if(message.method==="session/update"){this.structuredEvents+=1;this.lastProgressAt=new Date().toISOString();this.armStallTimer();const update=message.params?.update;const updateKind=typeof update?.sessionUpdate==="string"?update.sessionUpdate:"structured";const previousKind=this.lastEventType;this.lastEventType=updateKind;if(updateKind==="agent_message_chunk"&&update?.content?.type==="text"&&typeof update.content.text==="string"){const metaMessageId=update._meta?.["codebuddy.ai/messageId"];const messageId=typeof update.messageId==="string"?update.messageId:typeof metaMessageId==="string"?metaMessageId:"terminal-message";this.lastAgentMessageId=messageId;const existing=this.agentMessages.get(messageId)??"";this.agentMessages.set(messageId,(existing+update.content.text).slice(0,65536))}const milestone=(this.structuredEvents&this.structuredEvents-1)===0;if(milestone||updateKind!==previousKind){trace(this.options,"session_progress",{update_kind:updateKind,structured_events:this.structuredEvents,event_kind_changed:updateKind!==previousKind});this.currentState("running")}return}if(message.method)trace(this.options,"notification",{method:message.method})}consume(chunk){this.buffer+=chunk;for(;;){const boundary=this.buffer.indexOf("\n");if(boundary<0)return;const line=this.buffer.slice(0,boundary).trim();this.buffer=this.buffer.slice(boundary+1);if(!line)continue;try{this.handle(JSON.parse(line))}catch{trace(this.options,"unparseable_stdout",{bytes:Buffer.byteLength(line,"utf8")})}}}armStallTimer(){if(this.stallTimer)clearTimeout(this.stallTimer);this.stallTimer=setTimeout(()=>{void this.requestCancel("progress_saturated")},this.options.max_stall_seconds*1e3)}async requestCancel(reason){if(this.cancelRequested||!this.sessionId)return;this.cancelRequested=true;this.stopReason=reason;this.currentState("running");trace(this.options,"cancel_requested",{session_id:this.sessionId,reason});try{this.send({method:"session/cancel",params:{sessionId:this.sessionId}});this.cancelAcknowledged=true;this.currentState("running")}catch(error){trace(this.options,"cancel_request_error",{message:error instanceof Error?error.message:String(error)})}}async cancelWhenRequested(cancelPath){if(existsSync(cancelPath))await this.requestCancel("operator_cancel");return watch(path.dirname(cancelPath),(_event,filename)=>{if(filename?.toString()===path.basename(cancelPath)&&existsSync(cancelPath))void this.requestCancel("operator_cancel")})}fail(message){this.currentState("failed",message)}async run(){this.currentState("starting");const initialized=await this.request("initialize",{protocolVersion:1,clientInfo:{name:"codex-praetor",version:"1"},clientCapabilities:{fs:{readTextFile:true,writeTextFile:true},terminal:true}});trace(this.options,"initialized",{protocol_version:initialized.protocolVersion??null});const opened=await this.request("session/new",{cwd:path.resolve(this.options.cwd),mcpServers:[]});if(typeof opened.sessionId!=="string"||!opened.sessionId)throw new Error("ACP session/new returned no sessionId.");this.sessionId=opened.sessionId;this.currentState("running");this.armStallTimer();const cancelWatcher=await this.cancelWhenRequested(path.join(path.dirname(this.options.state_path),"cancel-request.json"));try{const terminal=await this.request("session/prompt",{sessionId:this.sessionId,prompt:[{type:"text",text:this.options.prompt}]});this.terminalStopReason=typeof terminal.stopReason==="string"?terminal.stopReason:"";if(typeof terminal.result==="string")this.terminalText=terminal.result;if(!this.terminalText&&this.lastAgentMessageId)this.terminalText=this.agentMessages.get(this.lastAgentMessageId)??"";const cancelled=this.cancelRequested&&this.terminalStopReason==="cancelled";if(this.terminalStopReason==="cancelled"&&!this.cancelRequested){const message="CodeBuddy ACP session ended as cancelled without a Codex cancellation request.";this.fail(message);trace(this.options,"terminal",{stop_reason:this.terminalStopReason,cancelled:false,unexpected_cancel:true});throw new Error(message)}this.currentState(cancelled?this.stopReason==="progress_saturated"?"progress_saturated":"cancelled_session_terminated":"completed");trace(this.options,"terminal",{stop_reason:this.terminalStopReason||null,cancelled,cancellation_reason:this.stopReason??null});if(!cancelled&&this.terminalText)process.stdout.write(`${this.terminalText}
`)}finally{cancelWatcher.close();if(this.stallTimer)clearTimeout(this.stallTimer);for(const terminal of this.terminalProcesses.values())terminal.kill();this.child.stdin?.end();if(!this.child.killed){const closed=new Promise(resolve=>this.child.once("close",()=>resolve()));this.child.kill();await Promise.race([closed,new Promise(resolve=>setTimeout(resolve,2e3))])}}}};async function main(){const options=readOptions();const runner=new AcpClient(options);try{await runner.run()}catch(error){const message=error instanceof Error?error.message:String(error);runner.fail(message);throw error}}main().catch(error=>{process.stderr.write(`${error instanceof Error?error.stack??error.message:String(error)}
`);process.exitCode=1});
