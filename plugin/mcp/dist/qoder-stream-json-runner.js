import{spawn}from"node:child_process";import{existsSync,readFileSync,watch}from"node:fs";import readline from"node:readline";import path from"node:path";import{renameSync,unlinkSync,writeFileSync}from"node:fs";var STATE_RENAME_RETRY_DELAYS_MS=[0,25,50,100,200];var stateWriteSequence=0;function isRetryableStateRenameError(error){const code=typeof error==="object"&&error!==null&&"code"in error?String(error.code):"";return code==="EPERM"||code==="EBUSY"||code==="EACCES"}function sleepSync(milliseconds){if(milliseconds<=0)return;const signal=new Int32Array(new SharedArrayBuffer(4));Atomics.wait(signal,0,0,milliseconds)}function replaceStateFile(temporary,target,dependencies={}){const rename=dependencies.rename??renameSync;const sleep=dependencies.sleep??sleepSync;let lastError;for(let attempt=0;attempt<STATE_RENAME_RETRY_DELAYS_MS.length;attempt+=1){try{rename(temporary,target);return}catch(error){lastError=error;if(!isRetryableStateRenameError(error)||attempt===STATE_RENAME_RETRY_DELAYS_MS.length-1)throw error;sleep(STATE_RENAME_RETRY_DELAYS_MS[attempt+1])}}throw lastError}function writeJsonStateFile(target,payload,dependencies={}){const temporary=`${target}.${process.pid}.${++stateWriteSequence}.tmp`;writeFileSync(temporary,`${JSON.stringify(payload)}
`,"utf8");try{replaceStateFile(temporary,target,dependencies)}catch(error){try{unlinkSync(temporary)}catch{}throw error}}function readOptions(){const index=process.argv.indexOf("--options-file");if(index<0||!process.argv[index+1])throw new Error("Missing --options-file for Qoder stream-json runner.");const value=JSON.parse(readFileSync(process.argv[index+1],"utf8").replace(/^\uFEFF/,""));if(value.schema!=="codex-praetor-qoder-stream-json-runner/v1"||!value.job_id||!value.cwd||!value.cli_path||!value.state_path||!Array.isArray(value.args)||!Number.isFinite(value.max_queue_seconds)||value.max_queue_seconds<1){throw new Error("Qoder stream-json runner options are incomplete.")}return value}function numberField(input,key){const value=input[key];return typeof value==="number"&&Number.isFinite(value)?value:void 0}async function main(){const options=readOptions();const cancelPath=path.join(path.dirname(options.state_path),"cancel-request.json");let totalLines=0;let parsedEvents=0;let invalidLines=0;const eventTypes=new Set;let queueObserved=false;let queueType="";let queueCount;let queueWaitElapsedMs;let queueMaxWaitMs;let serviceAvailable;let progressObserved=false;let cancelled=false;let queueTimedOut=false;let queueTimer;let cancellationWatcher;const child=spawn(options.cli_path,options.args,{cwd:options.cwd,stdio:["ignore","pipe","pipe"],windowsHide:true});const writeState=(state,error,exitCode2)=>{const payload={schema:"codex-praetor-qoder-stream-json-session/v1",job_id:options.job_id,connection_mode:"supervised_cli_stream_json",state,total_lines:totalLines,parsed_events:parsedEvents,invalid_lines:invalidLines,event_types:[...eventTypes],queue_observed:queueObserved,queue_type:queueType||void 0,queue_count:queueCount,queue_wait_elapsed_ms:queueWaitElapsedMs,queue_max_wait_ms:queueMaxWaitMs,service_available:serviceAvailable,progress_observed:progressObserved,cancelled,exit_code:exitCode2,updated_at:new Date().toISOString(),error};writeJsonStateFile(options.state_path,payload)};const stop=reason=>{if(reason==="queue")queueTimedOut=true;else cancelled=true;if(!child.killed)child.kill()};const armQueueTimer=()=>{if(queueTimer||progressObserved||cancelled)return;queueTimer=setTimeout(()=>{queueWaitElapsedMs=Math.max(queueWaitElapsedMs??0,options.max_queue_seconds*1e3);writeState("provider_queue_timeout","queue_wait_limit_exceeded");stop("queue")},options.max_queue_seconds*1e3)};const markProgress=()=>{progressObserved=true;if(queueTimer){clearTimeout(queueTimer);queueTimer=void 0}};writeState("starting");if(existsSync(cancelPath))stop("cancel");cancellationWatcher=watch(path.dirname(cancelPath),(_event,filename)=>{if(filename?.toString()===path.basename(cancelPath)&&existsSync(cancelPath))stop("cancel")});child.once("error",error=>writeState("failed",error.message));const stdout=child.stdout;const stderr=child.stderr;if(!stdout||!stderr)throw new Error("Qoder stream-json runner could not create stdio pipes.");stderr.pipe(process.stderr);const lines=readline.createInterface({input:stdout,crlfDelay:Infinity});lines.on("line",line=>{process.stdout.write(`${line}
`);if(!line.trim())return;totalLines+=1;try{const event=JSON.parse(line);parsedEvents+=1;const type=typeof event.type==="string"?event.type:typeof event.event==="string"?event.event:"untyped";eventTypes.add(type);const subtype=typeof event.subtype==="string"?event.subtype:"";if(type==="system"&&subtype==="model_queue_status"&&event.status==="queued"){queueObserved=true;queueType=typeof event.queue_type==="string"?event.queue_type:queueType;queueCount=numberField(event,"queue_count")??queueCount;queueWaitElapsedMs=numberField(event,"queue_wait_elapsed_ms")??queueWaitElapsedMs;queueMaxWaitMs=numberField(event,"queue_max_wait_ms")??queueMaxWaitMs;serviceAvailable=typeof event.service_available==="boolean"?event.service_available:serviceAvailable;armQueueTimer()}else if(type==="assistant"||type==="user"||type==="result"){markProgress()}writeState(queueTimedOut?"provider_queue_timeout":"running")}catch{invalidLines+=1;writeState(queueTimedOut?"provider_queue_timeout":"running")}});const exitCode=await new Promise(resolve=>child.once("close",resolve));if(queueTimer)clearTimeout(queueTimer);cancellationWatcher.close();if(queueTimedOut){writeState("provider_queue_timeout","queue_wait_limit_exceeded",exitCode);process.exitCode=2}else if(cancelled){writeState("cancelled_session_terminated",void 0,exitCode)}else if(exitCode===0){writeState("completed",void 0,exitCode)}else{writeState("failed","qoder_process_failed",exitCode);process.exitCode=exitCode??1}}main().catch(error=>{process.stderr.write(`${error instanceof Error?error.stack??error.message:String(error)}
`);process.exitCode=1});
