# Packaged MCP Placeholder

The final Codex plugin package should place the built MCP server here, for example:

```text
plugin/mcp/dist/server.js
```

Source code belongs in the repository-level `mcp/` folder. This folder represents the packaged distribution shape.

