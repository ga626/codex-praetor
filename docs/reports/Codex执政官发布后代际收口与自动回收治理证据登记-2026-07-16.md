# Codex 执政官发布后代际收口与自动回收治理证据登记

日期：2026-07-16
关联报告：[Codex执政官发布后代际收口与自动回收治理总报告-2026-07-16.md](Codex执政官发布后代际收口与自动回收治理总报告-2026-07-16.md)
边界：本登记记录本地考古和外部研究，不记录任何 token、账号数据库、完整私有日志或用户截图。

## 证据等级

- **A 级**：当前源码、当前本机状态、真实命令结果、官方正文、官方仓库 issue/源码正文。
- **B 级**：官方搜索摘要、成熟开源项目文档/issue、历史验收记录；存在版本或访问边界。
- **C 级**：多个 A/B 证据支持的工程判断；不是上游平台承诺。

## 一、本地项目考古

| ID | 等级 | 来源 | 事实 | 含义 |
|---|---|---|---|---|
| L01 | A | `D:\Projects\CodexPraetor`，`git status --short --branch`、`git log` | `main`/`origin/main` 为 `0a62eb7`，tag `v0.2.0-alpha`；本轮开始前工作树只有上一轮两份未跟踪报告 | 本轮没有把历史代码改动误认为当前实现 |
| L02 | A | `AGENTS.md: 发布交付边界、Release Generation And Closeout` | 已规定 immutable generation、branch isolated profile、stage/activate、fresh proof、provider readiness、active receipt 和 fail closed | 现有治理基础正确，但缺少 retirement/自动回收阶段 |
| L03 | A | `scripts/release/get-codex-praetor-generation.ps1` | generation identity 包含 version、git commit、skill/plugin/contract hash，生成 `generation_id` | 可以作为退休登记的主键 |
| L04 | A | `scripts/release/complete-codex-praetor-release.ps1:43-50,179-229,248-275` | stable cache 物理路径为 `cacheRoot\<version>`；stage 先安装/hash，再写 staged receipt；activate 需 fresh proof/readiness，最后写 active receipt | stage/activate 分层存在，但 cache path 与完整 generation 未完全分离 |
| L05 | A | `scripts/release/publish-codex-praetor-personal-cache.ps1:99-153` | 以 manifest version 作为 target path；成功后枚举其他 version 目录并调用 `Remove-Item -Recurse -Force`；删除失败只输出 warning | 直接删除旧 cache 是当前生命周期缺口 |
| L06 | A | `scripts/release/publish-codex-praetor-personal-marketplace.ps1:154-199` | 安装目录先 Move 到 backup，再移动 temp；成功后清理旧 backup；失败时尝试 rollback | 安装目录、cache、marketplace 不是一个统一退休队列 |
| L07 | A | `scripts/install/install-user.ps1:128-194` | 以临时目录 hash 校验后替换安装目录，保留 backup；输出提示重启 Codex或新 task | 用户入口仍把新 task 写成一般安装动作，未区分合同变化代际 |
| L08 | A | `scripts/verify/get-codex-praetor-health.ps1:95-155` | health 检查 active receipt、Skill/plugin/cache digest、marketplace、fresh context、provider readiness；不匹配则 blocked | 可以接入 retirement 状态，但当前 health 没有回收队列语义 |
| L09 | A | `scripts/verify/test-release-closeout.ps1` | 已测试无效 zip 不写 active receipt、stage 不 activate、activate、surface drift rejection | 可扩展测试 stage/activate 与 retirement 分离 |
| L10 | A | `scripts/verify/test-provider-capability-canary.ps1:19,82-83` | 全局 `ErrorActionPreference=Stop`；`& powershell @argsList 2>&1` 后读取 `$LASTEXITCODE` | stderr 可能先触发 PowerShell 错误，读取退出码顺序不安全 |
| L11 | A | Windows PowerShell 本机探针 | `5.1.19041.6456`、edition `Desktop`、`$PSNativeCommandUseErrorActionPreference` 不存在 | 不能依赖 PowerShell 7 native error preference |
| L12 | A | Qoder 隔离 worktree capability canary | 直接官方 CLI 返回 `CODEX_PRAETOR_CAPABILITY_CANARY_OK`；worktree/主仓库干净 | provider 能力通过，包装器错误不是 provider 无法执行 |
| L13 | A | 前一轮 Codex app-server/fresh context 记录 | reload 后 server `0.2.0-alpha`、17 tools；当前对话仍使用 `0.1.1-alpha` 旧路径；fresh context 17 tools passed | server reload、当前 conversation snapshot、cache path 是三个不同层 |
| L14 | A | `scripts/maintenance/clean-codex-praetor-runtime.ps1` | 只处理项目内 worker worktree/job/scratch；不处理用户 profile 的 plugin/cache generation | 现有 maintenance 不能承担插件代际回收 |

## 二、Codex 官方证据

| ID | 等级 | URL | 事实 | 结论边界 |
|---|---|---|---|---|
| O01 | A | [openai/codex#8957](https://github.com/openai/codex/issues/8957) | `mcpServer/refresh` 已实现，active thread 收到 pending refresh，后续 turn 重新初始化 MCP manager | reload 不是当前模型工具快照的同步替换 |
| O02 | A | [openai/codex#20605](https://github.com/openai/codex/issues/20605) | 旧 thread 看不到本地 MCP 工具变化，fresh thread/session 是 workaround | fresh context 是现实验收边界，Issue 仍非稳定 API 承诺 |
| O03 | A | [openai/codex#16653](https://github.com/openai/codex/issues/16653) | Skill/Plugin 变化当前 session 不会自动拾取，用户请求 reload commands | 不应每次编辑都强行重启当前 Codex |
| O04 | A | [openai/codex#21138](https://github.com/openai/codex/issues/21138) | Windows plugin cache 可能只按 manifest version 判断，源内容变更但 version 不变时继续使用 stale cache | 原地覆盖同 version cache 不安全 |
| O05 | A | [openai/codex#24390](https://github.com/openai/codex/issues/24390) | 已有 conversation 可能继续引用旧 plugin cache path | 旧 path 要有延迟回收和可诊断来源 |
| O06 | A | [openai/codex#25285](https://github.com/openai/codex/issues/25285) | Codex Desktop Windows 可能把 volatile cache hash 的绝对 Skill 路径持久化进长期 session；旧目录删除会使旧 session 丢失 Skill；作者观察到 junction 可恢复读取 | 强支持“旧 session 路径持久化”问题；兼容 junction 是 issue 作者的 workaround，不是 Codex Praetor 默认策略 |
| O07 | B | [openai/codex#4955](https://github.com/openai/codex/issues/4955) | MCP server 独立重启能力曾被请求；server 生命周期和 connection manager 绑定 | MCP server restart 与 session/tool snapshot 分开处理 |
| O08 | B | [openai/codex#26934](https://github.com/openai/codex/issues/26934) | curated plugin stale cache 清理已合并，范围主要是被移除 curated plugin | 不等于运行中任意旧 cache 可强制删除 |

## 三、Windows 和更新器证据

| ID | 等级 | URL | 事实 | 对本项目的用法 |
|---|---|---|---|---|
| W01 | A | [MoveFileExW](https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-movefileexw) | 可用 `MOVEFILE_DELAY_UNTIL_REBOOT` 登记后续删除/移动；目录只有为空时才会在重启删除；调用返回不代表重启时操作最终成功 | 说明延迟删除是操作系统级兜底，不是当前发布激活的替代；回收器必须记录最终结果 |
| W02 | A | [About Restart Manager](https://learn.microsoft.com/en-us/windows/win32/rstmgr/about-restart-manager) | 安装器可识别占用文件的应用并在有权限时停止/重启；关键应用可能无法停止 | 可用于只读占用诊断；Codex Praetor 不得自动停止当前 Codex |
| W03 | A | [RmRegisterResources](https://learn.microsoft.com/en-us/windows/win32/api/restartmanager/nf-restartmanager-rmregisterresources) | 可登记文件名、服务或进程资源供 Restart Manager 查询 | 未来回收器可记录 blocker 进程 |
| W04 | A | [RmGetList](https://learn.microsoft.com/en-us/windows/win32/api/restartmanager/nf-restartmanager-rmgetlist) | 返回当前使用已登记资源的应用和服务 | 可辅助解释 `blocked_by_process`，不能据此强杀 |
| W05 | B | [Squirrel update process](https://github.com/Squirrel/Squirrel.Windows/blob/master/docs/using/update-process.md) | 新版本放进新的 `app-version` 目录；下一次启动只保留 current 和 immediately previous，其余清理 | 成熟更新器采用并行目录和下一次启动清理 |
| W06 | B | [Squirrel issue #589](https://github.com/Squirrel/Squirrel.Windows/issues/589) | 更新后正在使用的旧目录会暂时保留，后续启动再清理 | 旧目录暂留是正常生命周期，不是发布失败 |
| W07 | B | [WinSW deferred file operations](https://github.com/winsw/winsw/blob/v3/docs/deferred-file-operations.md) | Windows 无法覆盖 in-use 文件；WinSW 将操作放进 deferred file 并在目标进程启动前执行，记录成功/失败 | 支持 deferred retirement queue 模式 |
| W08 | B | [MoveFileEx/Windows delayed operations search evidence](https://learn.microsoft.com/en-us/sysinternals/downloads/pendmoves) | Windows 的 PendingFileRenameOperations 由 Session Manager 在启动前处理 | 只能作为最终系统重启兜底，不能误报立即清理完成 |

## 四、PowerShell 证据

| ID | 等级 | URL | 事实 | 结论 |
|---|---|---|---|---|
| P01 | A | [about_Error_Handling](https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_error_handling?view=powershell-7.6) | native 程序用非零 exit code 报告失败，PowerShell 存到 `$LASTEXITCODE`；默认非零不会自动成为 ErrorRecord；7.3/7.4 才引入/稳定 native preference | 5.1 下必须显式读 exit code |
| P02 | A | [about_Redirection](https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_redirection?view=powershell-7.6) | `2>&1` 把 Error stream 合并到 Success stream | 不得用合流 stream 判断 native 成功 |
| P03 | B | [Start-Process](https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.management/start-process?view=powershell-7.6) | 支持独立 `-RedirectStandardOutput`、`-RedirectStandardError`、`-Wait`、`-PassThru` | 可作为兼容实现 |
| P04 | B | [ProcessStartInfo](https://learn.microsoft.com/en-us/dotnet/api/system.diagnostics.processstartinfo?view=net-10.0) | `ProcessStartInfo` 可配置进程启动和重定向；配合 `RedirectStandardError`/`RedirectStandardOutput` 可分流 | 更适合作为项目统一 helper 的底层 |

## 五、方案判断与证据缺口

| ID | 等级 | 判断 | 证据链 | 缺口 |
|---|---|---|---|---|
| D01 | C | cache publisher 不能在成功发布后直接删除所有非当前 version | L04-L07、O04-O06、W05-W07 | 本 PR 已实现退休登记、延迟回收和隔离回归测试 |
| D02 | C | 旧 generation 应不可变保留、退休登记、自动重试回收 | L03-L09、W01-W07 | Codex 内部 session 是否仍有未来引用无法由本项目完全读取 |
| D03 | C | 回收器可自动清理“释放占用后的旧目录”，但不能保证任意历史 session 永久可恢复 | O06、W01-W04、W06 | 需要后续实测 Restart Manager 对 Codex MCP/Skill 文件的识别覆盖 |
| D04 | C | “每 generation 一次 fresh context”是可接受的人机边界 | O01-O03、L13 | Codex 未来若提供稳定 reload API，需要重新评估是否可降级该动作 |
| D05 | A | Qoder 能力不是第二个问题的根因 | L10-L12、P01-P02 | 需要用统一 helper 回归所有 native invocation |
| D06 | C | 第二个问题必须统一 helper + matrix，而非局部删 `2>&1` | L10、P01-P04 | 需确定 helper 文件位置和参数引用约定 |
| D07 | C | 用户级 Task Scheduler 是自动重试的可行触发器 | W01-W04，微软 Task Scheduler 文档：[New-ScheduledTaskTrigger](https://learn.microsoft.com/en-us/powershell/module/scheduledtasks/new-scheduledtasktrigger?view=windowsserver2025-ps)、[schtasks](https://learn.microsoft.com/en-us/windows/win32/taskschd/schtasks) | 需要安装/卸载权限、用户 profile 移动和升级路径测试 |

## 六、KnowledgeRadar 研究记录

- `access_path=native_mcp`。
- 先调用 `mcp__knowledgeradar__health_check(mode="summary")` 和 `mcp__knowledgeradar__get_capabilities(summary=true)`；health `ok`，工具数 17。
- 调用 `mcp__knowledgeradar__kr_research(mode="deep_route", budget="deep")` 获得 `generic_web_ecology` 和 `github_repository_ecology` 路线。
- 调用 `kr_web_search` 检索 Codex 官方 issue、Windows API、PowerShell、Task Scheduler、Squirrel、WinSW；调用 `search_github_repositories` 做 GitHub 生态补充；调用 `extract_web_page` 对关键 GitHub/Microsoft/开源文档做正文抽取。
- 一次 `limit=12` 超出 `kr_research` 接口上限并被本地 validation 拒绝；改为 `limit=10` 后成功，未影响后续研究。
- Tavily 当日额度已耗尽，web 主要使用 `anysearch`；没有把内置 web 当作绕过 KR 的独立路线。
- Microsoft Learn 部分页面有授权提示，但仍抽取到官方正文片段；登记中将搜索摘要/受限正文标为 B 或明确说明缺口。
- 深度路线建议的 `get_task_status(compact=true)`、`analyze_decision_logs(compact=true)` 已有前一轮记录：无 active/stale，最近 20 条决策日志 20 success、0 failure；本轮没有产生需要等待的后台媒体任务。
