# 证据登记

日期：2026-07-16
调查对象：`D:\Projects\CodexPraetor` 与 2026-07-15 真实外部 worker 派工失败

## 证据强度

- **A 级**：真实会话底层事件、当前源码、当前本机状态、官方文档正文。
- **B 级**：Git 历史、历史验收记录、官方搜索摘要，可支持判断但需注意版本和时间边界。
- **C 级**：由多个 A/B 级证据形成的强推断，尚缺一个决定性历史样本。

## 本地项目与安装态

| ID | 强度 | 来源 | 已提取事实 | 用途 |
|---|---|---|---|---|
| L01 | A | `D:\Projects\CodexPraetor` Git | `main`、`origin/main` 和 tag `v0.1.3-alpha` 均指向 `a96a925`；当前工作区位于未授权分支 `codex/fix-mcp-probe-project-root`，提交 `3549d90`，工作树无未提交改动 | 判断公开版本、主线和本任务违规修改边界 |
| L02 | A | `gh release view v0.1.3-alpha` | GitHub prerelease 于 2026-07-15 发布；安装资产 `codex-praetor-setup-0.1.3-alpha.zip`，远端 digest `cd75df497cd49af05e78a92c1dcf1cdae4712019baf327f2476cc631ec6415aa` | 证明公开交付版本 |
| L03 | A | GitHub PR/issue/run 查询 | 当前无开放 PR、无开放 issue；`main`、tag 和未授权分支最近 CI 均为 success | 证明“全绿但真实派工失败” |
| L04 | A | `C:\Users\ga990\.codex\skills\codex-praetor` 与源码逐文件 SHA256 | installed skill 的 18 个文件与当前源码 skill 全部一致 | 证明 Skill 已是新内容 |
| L05 | A | `C:\Users\ga990\plugins\codex-praetor` | plugin manifest 与 MCP package 均为 `0.1.1-alpha` | 证明本机 plugin 过旧 |
| L06 | A | `C:\Users\ga990\.codex\plugins\cache\personal\codex-praetor` | cache 只有 `0.1.0-alpha`、`0.1.1-alpha` | 证明 personal cache 未更新到 0.1.3 |
| L07 | A | 当前 Codex 原生 MCP 工具面 | 只暴露 8 个旧工具：route、dry-run、plan、jobs/lanes/status/conflicts；源码 0.1.2 新增的 dispatch/result/next-ready/dispatch-plan-task/verify-task 不可见 | 证明当前线程运行的是旧 MCP |
| L08 | A | `C:\Users\ga990\.codex\codex-praetor.onboarding-state.json`，只提取非敏感状态 | Qoder=`not_selected/not_run`；CodeBuddy=`not_selected/not_run`；MiMo=`cli_rechecked/not_run` | 证明三 provider 均无通过 canary 的可路由状态 |
| L09 | A | `C:\Users\ga990\.codex\codex-praetor.local.json`，只提取 CLI 路径 | 尽管 Qoder/CodeBuddy 未选择，旧路径仍保留；wrapper 会读取这些路径 | 证明 onboarding 与实际路由状态分裂 |
| L10 | A | 本机 CLI 版本探针 | Qoder `1.0.39`；CodeBuddy `2.106.4`；MiMo `0.1.0` | 固定本次兼容性判断的版本边界 |
| L11 | A | WorkBuddy 内嵌 CLI 元数据 | CLI 目录创建于 2026-07-15 12:28:56；失败发生于 12:39；`package.json` 的发布版本为 `2.106.4` | 支持“失败前刚发生 provider 组件更新”的时间关联 |
| L12 | A | `scripts\dispatch\invoke-codex-praetor.ps1:750` | 自动路由只根据昼夜、默认 tier 和价格偏好选择 provider，不读取 onboarding、canary、最近失败或冷却状态 | 健康盲路由根因 |
| L13 | A | `setup.ps1:718` | setup 只更新本轮选中的 provider 路径，不清理未选 provider 的旧配置 | 旧 CodeBuddy 路径继续被自动选择的根因之一 |
| L14 | A | `scripts\dispatch\invoke-codex-praetor.ps1:937` | CodeBuddy readonly 命令固定为 `-y --tools Read,Glob,Grep` | 复现工具集合同 |
| L15 | A | WorkBuddy `dist\codebuddy.js` 本地代码 | `resolveFunctionOrHandoff` 在模型请求的工具不在 tool map 时抛出 `Tool <name> not found in agent <agent>.` | 证明该错误是逻辑工具集合不匹配，不是“缺少 Git Bash” |
| L16 | A | `scripts\dispatch\invoke-codex-praetor.ps1:821` 与 tier JSON | MiMo readonly 的 `agent` 为空；CLI 不传 `--agent` | 证明 MiMo 实际落到默认 agent |
| L17 | A | `scripts\dispatch\invoke-codex-praetor.ps1:842` | worktree 名只精确到秒；两个同 provider/tier 的同秒任务会得到同名路径和分支 | MiMo 并发冲突直接根因 |
| L18 | A | `scripts\dispatch\invoke-codex-praetor.ps1:579` | blocking 模式直接运行 CLI，没有 job ID、job.json、stdout/stderr 文件或 completion.json | 解释为什么当时无法可靠查询、取消和验收 |
| L19 | A | `scripts\dispatch\watch-codex-praetor-job.ps1:199` | background watcher 使用 `Start-Process -Wait -PassThru`，`pid` 在等待结束后才写回；没有 worker 超时 | 证明后台生命周期同样不完整 |
| L20 | A | `mcp\src\powershell.ts:23` | MCP 超时只执行 `child.kill()`，未保证终止后代进程树 | 解释超时后的残留风险 |
| L21 | A | `mcp\src\tools.ts:161` | 结果分类不识别 `Tool ... not found in agent ...`；exit code 0 且 completion=completed 会落入“等待 Codex 验收” | 证明语义失败不会自动转为失败 |
| L22 | A | `.github\workflows\ci.yml` 与 `scripts\verify\test-codex-praetor.ps1:20` | CI 调默认产品验证；默认会自动 `SkipDryRun=true`，不会运行 provider dry-run，更不会运行真实 canary | 证明 CI success 不是真实 provider 通过 |
| L23 | A | `mcp\src\self-test.ts:47` | 对真实 dispatch 仅断言 `typeof dispatchTool === function`；plugin smoke 主要检查工具存在和 dry-run | 证明新增真实入口没有被执行测试 |
| L24 | A | `scripts\verify\test-provider-readonly-canary.ps1` | canary 本身会检查固定 marker 和主 repo 状态，但默认是 preview，且结果不参与自动路由 | 说明已有机制为什么没保护真实用户 |
| L25 | A | `git worktree list` / 本机分支 | 当前 CodexPraetor repo 仍挂有 9 个 2026-07-10 MiMo readonly worktree | 证明运行产物清理依赖人工维护 |

## 真实会话证据

原始会话：

`C:\Users\ga990\.codex\sessions\2026\07\02\rollout-2026-07-02T13-46-22-019f215d-1a76-7c92-a338-92bd585d033e.jsonl`

只按 JSON 事件类型和 call ID 定向解析，没有整段倾倒会话。

| ID | 强度 | JSONL 行 | 事实 |
|---|---|---|---|
| S01 | A | 9410 | 用户要求后续开发使用 skill 分配任务给其他 Agent |
| S02 | A | 9416 | 助手读取 installed `codex-praetor` Skill |
| S03 | A | 9424-9428 | 两个 CodeBuddy dry-run 成功；输出只证明命令拼装 |
| S04 | A | 9435-9438 | 两个 CodeBuddy 以 blocking 模式并行启动 |
| S05 | A | 9454、9459 | 两个任务都输出 `Tool Bash not found in agent cli.`，进程退出码仍为 0 |
| S06 | A | 9465-9466 | 助手调用不存在的 `-Help` 参数并失败 |
| S07 | A | 9475-9478 | MiMo 与 Qoder dry-run 均成功 |
| S08 | A | 9484-9487 | 两个 MiMo 同秒创建同名 worktree；第二个以 branch already exists 失败 |
| S09 | A | 9486、9494、9499 | 第一个 MiMo 只有启动信息，后续两次等待均无输出；没有任何“权限自动拒绝”工具输出 |
| S10 | A | 9491-9492 | 助手无证据宣称“遇到权限请求被自动拒绝” |
| S11 | A | 9505-9506 | `write_stdin` 后端不支持 Ctrl+C 中断 |
| S12 | A | 9512-9538 | 助手按命令行字符串找进程并连续强杀；后续查询命令会匹配自身 PowerShell |
| S13 | A | 9542-9543 | 助手误称“MiMo watcher 反复拉起”；实际运行是 blocking，没有 watcher |
| S14 | A | 9556 | 助手最终承认剩余 PowerShell 是查询命令自身，不是 worker |
| S15 | A | 9567 以后 | 无证据的“权限拒绝”结论被写入下游长期规划报告，形成持久化误诊 |

## 历史修复与验收证据

| ID | 强度 | 来源 | 事实 |
|---|---|---|---|
| H01 | A | commit `a6c9741` | 所谓“Fix MiMo readonly dispatch”主要把 MiMo `agent=plan` 改为空、强化 task packet、压平换行并确保 worktree；没有修 CodeBuddy 工具集合、健康路由、超时取消或版本一致性 |
| H02 | B | 2026-07-08/09 evidence register | 记录过 CodeBuddy `CODEBUDDY_EFFORT_OK`、`WRAPPER_AUTH_OK`、`BACKGROUND_OK_2` 和 readonly 工具路线；没有保存当时精确 CLI 版本与完整 capability tuple |
| H03 | B | 2026-07-10 验收 rollout | 验收结论明确承认 Qoder/MiMo 版本探针只是信息，未来应运行真实 provider canary；但发布门禁没有落实这一点 |
| H04 | A | Git 历史 `af1a84b` / `a96a925` | 0.1.2 增加真实 dispatch/result/orchestration loop；0.1.3 主要做验证边界和可复现发布，不是 provider 兼容修复 |

## 官方外部资料

| ID | 强度 | 来源 | 已提取事实 | 对整改的含义 |
|---|---|---|---|---|
| O01 | A | [CodeBuddy CLI Reference](https://www.codebuddy.ai/docs/zh/cli/cli-reference) | `--tools` 是可见内置工具白名单；`--allowedTools` / `--disallowedTools` 是权限规则；Bash 是正式工具；官方还有 `ps/logs/attach/kill` 和 `--bg --name` | 不应把“隐藏工具”和“拒绝工具”混为一谈；可评估 provider 原生生命周期接口 |
| O02 | A | [CodeBuddy v2.106.4](https://www.codebuddy.ai/docs/cli/release-notes/v2.106.4) | 本机版本对应 2.106.4；该版未声明修复工具列表恢复或 Glob 挂起 | 固定本次失败版本 |
| O03 | A | [CodeBuddy v2.106.5](https://www.codebuddy.ai/docs/cli/release-notes/v2.106.5) | 修复工具调用失败后恢复提示中的工具列表过时、并行 Glob 永久挂起、`--help` 挂起等 | 证明 2.106.4 附近的工具恢复/并发链路确有官方已知问题，但不能自动证明本次完全同因 |
| O04 | A | [CodeBuddy v2.106.6](https://www.codebuddy.ai/docs/cli/release-notes/v2.106.6) | 继续修复命令执行兼容、模型工具调用稳定性和后台任务输出读取 | 升级值得纳入矩阵，但必须 canary 后再放行 |
| O05 | A | [MiMo Agents](https://mimo.xiaomi.com/mimocode/agents) | Build 是默认 primary agent 且全部工具开启；Plan 是受限分析 agent；官方推荐用 agent permission 精细配置 edit/bash/webfetch | 证明当前 `agent=""` 不能称为技术性 readonly |
| O06 | A | [Qoder Permissions](https://docs.qoder.com/en/cli/permissions) | `--tools` 限制内置工具；allow/deny/ask 与 permission mode 分层；Plan 是独立只读工作状态 | 支持把 capability 与 permission profile 一起做版本化 canary |
| O07 | B | [Circuit Breaker Pattern](https://learn.microsoft.com/en-us/azure/architecture/patterns/circuit-breaker) | 故障超过阈值后应临时阻断访问，避免反复调用明显失败的远端服务，并在恢复窗口后探测 | 支持 provider cooldown/half-open canary 设计；是架构类比，不是 provider 专属规范 |
| O08 | B | [Health Endpoint Monitoring](https://learn.microsoft.com/en-us/azure/architecture/patterns/health-endpoint-monitoring) | 健康检查应验证服务是否真正执行所需功能，而不只验证进程存在 | 支持真实 capability canary，而非 `--version` 或 dry-run |
| O09 | B | [.NET Process.Kill](https://learn.microsoft.com/en-us/dotnet/api/system.diagnostics.process.kill) | `Kill(Boolean)` 可选择终止子/后代进程 | 支持完整进程树取消，不再按字符串扫进程 |
| O10 | B | [Windows Job Objects](https://learn.microsoft.com/en-us/windows/win32/procthread/job-objects) | Job Object 可将一组进程作为单元管理和终止 | Windows 下更强的 worker 进程树所有权方案 |
| O11 | B | [.NET Process.WaitForExit](https://learn.microsoft.com/en-us/dotnet/api/system.diagnostics.process.waitforexit) | 可设置等待期限；无期限等待会阻塞线程 | 支持 worker timeout 必须是一等参数 |
| O12 | B | [GitHub Actions Matrix](https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/run-job-variations) | matrix 可基于多个变量组合自动产生测试 job | 支持 provider/version/model/agent/tool-profile 验收矩阵 |

## 强推断与缺口

| ID | 强度 | 推断 | 缺口 |
|---|---|---|---|
| I01 | C | 7 月 15 日 WorkBuddy 内嵌 CLI 更新使既有 CodeBuddy readonly 命令出现兼容回归 | 旧成功时未记录精确 CodeBuddy CLI 版本，不能断言升级是唯一原因 |
| I02 | C | CodeBuddy 默认 agent/模型仍预期 Bash 可用，而 wrapper 用 `--tools` 隐藏了 Bash，导致工具调用合同破裂 | 没有 provider 端原始模型 response/system prompt，无法确认 Bash 调用由哪条提示触发 |
| I03 | C | 0.1.2/0.1.3 功能增长后，安装同步和验收门禁未同步升级，是“越来越差”的系统性原因 | 需要安装器历史日志才能精确判定 plugin 为什么停在 0.1.1 |

## KnowledgeRadar 路径

- `access_path=native_mcp`
- 已执行 `health_check(mode="summary")`、`get_capabilities(summary=true)`、`kr_research(mode="deep_route", budget="deep")`。
- 外部搜索和正文抽取均由 `mcp__knowledgeradar` 路由；没有绕过为独立 web 路线。
- Microsoft Learn 正文静态抽取在当前环境被错误分类为空/blocked，因此相关条目以官方搜索摘要和稳定 API 文档 URL 作为 B 级证据；CodeBuddy、MiMo、Qoder 正文抽取成功，为 A 级。
